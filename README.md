# My Portfolio  
> A personal portfolio website built with Node.js + Express + EJS to showcase projects & skills.

## ✅ Features  
- Uses **Express.js** as the backend web server.  
- Uses **EJS** (Embedded JavaScript) as the templating engine to dynamically render HTML views.  
- Structured folder layout with `models`, `views`, `public` directories.  
- Public folder for static assets (CSS, images, JavaScript).  
- Clean, responsive design to present projects in a professional way.


